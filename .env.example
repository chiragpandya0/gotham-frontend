VITE_API_BASE_URL=http://localhost:8000

# Free key from https://carto.com/basemaps/apikey/ (no approval queue, 5M
# tile requests/month free). Without it, CARTO's basemap tiles render an
# "API key required" watermark instead of the actual map.
VITE_CARTO_API_KEY=
